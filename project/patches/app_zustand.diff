--- a/project/App.tsx
+++ b/project/App.tsx
@@
-import React, { useEffect, useMemo, useState } from 'react';
+import React, { useEffect, useMemo, useState } from 'react';
@@
 import {
@@
 } from 'lucide-react';
+import { useAppStore } from './store';
@@
-  const [isLoggedIn, setIsLoggedIn] = useState(false);
-  const location = useLocation();
-  const navigate = useNavigate();
-  const [userRole, setUserRole] = useState<UserRole>(UserRole.AssetAdmin);
-  const [currentView, setCurrentView] = useState<View>(() => {
-    const view = pathToView[window.location.pathname];
-    return view ?? 'cockpit';
-  });
-  const [isBigScreen, setIsBigScreen] = useState(false);
-  const [sidebarOpen, setSidebarOpen] = useState(false);
+  const location = useLocation();
+  const navigate = useNavigate();
+
+  const {
+    isLoggedIn,
+    userRole,
+    currentView,
+    isBigScreen,
+    sidebarOpen,
+    setLoggedIn,
+    setCurrentView,
+    setBigScreen,
+    setSidebarOpen,
+  } = useAppStore();
@@
-  const handleLogin = (role: UserRole) => {
-    setUserRole(role);
-    setIsLoggedIn(true);
+  const handleLogin = (role: UserRole) => {
+    setLoggedIn(true, role);
@@
-      setCurrentView('commercial');
+      setCurrentView('commercial');
@@
-  if (isBigScreen) {
-    return <BigScreen onExit={() => setIsBigScreen(false)} />;
+  if (isBigScreen) {
+    return <BigScreen onExit={() => setBigScreen(false)} />;
@@
-      case 'cockpit': return <Dashboard userRole={userRole} onEnterBigScreen={() => setIsBigScreen(true)} />;
+      case 'cockpit': return <Dashboard userRole={userRole} onEnterBigScreen={() => setBigScreen(true)} />;
@@
-                    onClick={() => setSidebarOpen(true)}
+                    onClick={() => setSidebarOpen(true)}
@@
-            setCurrentView(view as View);
-            if (window.innerWidth < 768) {
-              setSidebarOpen(false);
-            }
+            setCurrentView(view as View);
+            if (window.innerWidth < 768) {
+              setSidebarOpen(false);
+            }
@@
-            setCurrentView(view);
-            if (window.innerWidth < 768) {
-              setSidebarOpen(false);
-            }
+            setCurrentView(view);
+            if (window.innerWidth < 768) {
+              setSidebarOpen(false);
+            }
