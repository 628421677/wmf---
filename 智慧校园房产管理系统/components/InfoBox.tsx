import React from 'react';
// This component is now deprecated and replaced by the RightSidebar for detailed views.
// Keeping an empty render or removing functionality to avoid errors if imported elsewhere, 
// though MapView no longer uses it.
export const InfoBox: React.FC<any> = () => {
  return null;
};
